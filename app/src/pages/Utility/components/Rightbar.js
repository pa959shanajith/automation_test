import React from 'react';
import {ReferenceBar } from '../../global';

const Rightbar=()=>{
    const hideInfo = true ;
    return (
            <ReferenceBar 
                hideInfo={hideInfo} 
            />
           
            )
}

export default Rightbar;
